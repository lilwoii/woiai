# Electron wrapper (minimal)

This folder is a minimal wrapper to launch the React UI in an Electron window.

## Run
```bash
cd electron_app
npm install
npm run start
```

Make sure backend is running on http://127.0.0.1:8000 and frontend dev server on http://localhost:3000,
or build the frontend and update `main.js` accordingly.
